# ОТЧЁТ ПО ЛАБОРАТОРНОЙ РАБОТЕ №4

---

## Титульный лист

**Министерство науки и высшего образования Российской Федерации**  
Федеральное государственное автономное образовательное учреждение высшего образования  
**«Национальный исследовательский университет ИТМО»**

Факультет программной инженерии и компьютерной техники  

**Дисциплина:** Тестирование программного обеспечения  

**Лабораторная работа №4**  
**Нагрузочное и стресс-тестирование (Apache JMeter)**

**Выполнил:** Лазарев Дмитрий Иванович  

**Группа:** P3330  

**Преподаватель:** *ФИО*  

**Санкт-Петербург, 2026**

---

## 1. Текст задания

С помощью Apache JMeter провести **нагрузочное** и **стресс-тестирование** веб-приложения.

**Параметры варианта:**


| Параметр                   | Значение                                                 |
| -------------------------- | -------------------------------------------------------- |
| Config 1                   | $4100 — `token=492556914`, `user=2109717879`, `config=1` |
| Config 2                   | $7400 — `config=2`                                       |
| Config 3                   | $10500 — `config=3`                                      |
| Параллельных пользователей | 12                                                       |
| Нагрузка на пользователя   | 20 запр./мин                                             |
| Макс. время отклика (SLA)  | **640 мс**                                               |
| Целевая суммарная нагрузка | 240 запр./мин (4 RPS)                                    |


**Коды ответов:** HTTP 403 — неверные параметры; HTTP 503 — перегрузка сервера.

---

## 2. Описание конфигурации JMeter для нагрузочного тестирования

### 2.1. Схема тест-плана

```
Test Plan (Run Thread Groups consecutively = true)
├── Thread Group "conf #1"  → GET config=1
├── Thread Group "conf #2"  → GET config=2
└── Thread Group "conf #3"  → GET config=3
```

Конфигурации тестируются **последовательно**, чтобы не влиять друг на друга.

### 2.2. Параметры каждой Thread Group


| Параметр                  | Значение                                                  |
| ------------------------- | --------------------------------------------------------- |
| Number of Threads (users) | **12**                                                    |
| Ramp-Up Period            | **60 с**                                                  |
| Loop Count                | **40**                                                    |
| HTTP Server               | `localhost:8083` (SSH-туннель → `stload.se.ifmo.ru:8080`) |
| Constant Throughput Timer | **20** samples/min, calcMode = *this thread only*         |
| Response Assertion        | HTTP response code = **200**                              |


**Важно:** проверка SLA (640 мс) выполняется **при анализе результатов** по полю `elapsed` в CSV, а не через Duration Assertion. Иначе JMeter показывает «Error %» даже при успешных HTTP 200 — это не ошибки сервера, а превышение времени отклика.

### 2.3. Инструменты

- Apache JMeter **5.6.3**
- Java 25
- SSH-туннель: `localhost:8083` → `stload.se.ifmo.ru:8080`

---

## 3. Результаты нагрузочного тестирования

**Источник данных:** `load/load.csv` (1440 запросов, 480 на конфигурацию)  
**Дата прогона:** июнь 2026

### 3.1. Сводная таблица


| Config | Цена   | Avg, мс  | Min, мс | Max, мс | 90% Line, мс | Запросов ≤640 мс    | HTTP 200 | Throughput, RPS | Error % (JMeter) |
| ------ | ------ | -------- | ------- | ------- | ------------ | ------------------- | -------- | --------------- | ---------------- |
| **1**  | $4100  | **1418** | 1322    | 3161    | 1464         | **0 / 480 (0%)**    | 480      | 2.79            | **0%**           |
| **2**  | $7400  | **991**  | 917     | 2101    | 1054         | **0 / 480 (0%)**    | 480      | 2.79            | **0%**           |
| **3**  | $10500 | **711**  | 518     | 6312    | 639          | **433 / 480 (90%)** | 480      | 2.74            | **0%**           |


**Фактическая нагрузка:** ~~167 запр./мин на конфигурацию (~~2.8 RPS) вместо целевых 240 запр./мин.

**HTTP-ошибок нет:** все 1440 ответов — код **200**, `success=true` в JMeter (**Error % = 0%**).

### 3.2. Интерпретация


| Config       | Поведение                                                                                                           |
| ------------ | ------------------------------------------------------------------------------------------------------------------- |
| **Config 1** | Минимальный отклик 1322 мс — в **2+ раза** выше SLA. Не подходит.                                                   |
| **Config 2** | Минимум 917 мс — всё ещё выше 640 мс. Не подходит.                                                                  |
| **Config 3** | **90%** запросов укладываются в 640 мс; P90 = 639 мс. Единственная рабочая конфигурация. Отдельные пики до 6312 мс. |


### 3.3. Графики

*Вставить скриншоты из `load_report/index.html` или JMeter GUI: Aggregate Report / Response Time Graph для config 1, 2, 3.*

---

## 4. Выбор конфигурации аппаратного обеспечения

### Выбранная конфигурация: **Config 3 ($10500)**

### Обоснование

1. **Config 1 ($4100)** — не удовлетворяет требованию: ни один из 480 запросов не уложился в 640 мс (min = 1325 мс).
2. **Config 2 ($7400)** — не удовлетворяет требованию: все запросы > 640 мс (min = 921 мс).
3. **Config 3 ($10500)** — **единственная** конфигурация, при которой 90% запросов (433/480) укладываются в SLA; P90 = 639 мс.

Среди конфигураций, **удовлетворяющих** (или хотя бы частично удовлетворяющих) требованию по времени отклика при заданной нагрузке, **дешевле Config 3 нет** — Config 1 и 2 отпадают полностью.

> **Вывод для стресс-теста:** далее тестируем **config=3** (уже задано в `stress/stress-test-plan.jmx`).

---

## 5. Описание конфигурации JMeter для стресс-тестирования


| Параметр           | Значение                               |
| ------------------ | -------------------------------------- |
| Config             | **3** (выбран на шаге 4)               |
| Number of Threads  | **48** (нагрузка выше номинальных 12)  |
| Ramp-Up            | **240 с**                              |
| Duration           | **300 с**                              |
| Throughput Timer   | 20 запр./мин на поток                  |
| Duration Assertion | **640 мс** (для фиксации точки отказа) |
| Response Assertion | HTTP 200                               |


Цель: найти число потоков / нагрузку, при которой среднее или максимальное время отклика превышает 640 мс или появляется HTTP 503.

---

## 6. График зависимости времени отклика от нагрузки

**Источник:** `stress/stress.csv` → график `stress/stress-graph.png` (скрипт `plot-stress.py`)

График стресс-теста

### 6.1. Результаты стресс-теста (config=3)


| Показатель                              | Значение                                                       |
| --------------------------------------- | -------------------------------------------------------------- |
| Всего запросов                          | **5900**                                                       |
| HTTP 200                                | **5900 (100%)** — перегрузки (503) не было                     |
| HTTP 403 / 503                          | **0**                                                          |
| Макс. число потоков                     | **48** (план: ramp-up 240 с, duration 300 с)                   |
| Номинальная нагрузка (нагрузочный тест) | **12 потоков**, ~167 запр./мин                                 |
| **Точка отказа по SLA**                 | **~16 потоков** — более 50% запросов > 640 мс                  |
| При 20+ потоках                         | 100% запросов превышают 640 мс (avg до 1457 мс при 48 потоках) |


### 6.2. Анализ графика

- При малой нагрузке (1–2 потока) среднее время ~570–584 мс — в пределах SLA.
- С ростом нагрузки время отклика растёт; **пик среднего** на графике — **~18 потоков, 636 мс** (ещё ниже линии SLA 640 мс).
- График строится только по запросам с `success=true` (прошедшим Duration Assertion). На высокой нагрузке большинство запросов помечаются как failed из‑за превышения 640 мс, поэтому точки на графике обрываются около 19 потоков.
- **Вывод:** система выдерживает номинальные **12 потоков**, но запас прочности невелик; при **16+ потоках** SLA систематически нарушается.


| Метрика               | Значение                                               |
| --------------------- | ------------------------------------------------------ |
| Номинальная нагрузка  | 12 потоков — укладывается в SLA (90% запросов ≤640 мс) |
| Точка отказа (стресс) | **~16 потоков** (>50% запросов >640 мс)                |


---

## 7. Общие выводы

1. При нагрузочном тесте все конфигурации отвечали **HTTP 200**, **Error % = 0%** в JMeter.
2. **Config 1 и 2** не справляются с SLA 640 мс при 12 пользователях.
3. **Config 3 ($10500)** — единственный рабочий вариант: 90% запросов в пределах SLA.
4. Стресс-тест (config 3): перегрузки сервера (503) не обнаружено; точка отказа по времени отклика — **~16 параллельных потоков**.
5. При 48 потоках среднее время отклика вырастает до **~1457 мс** — система сильно деградирует, но продолжает отвечать HTTP 200.

---

## Приложение: команды

```bat
scripts\tunnel.bat
set JMETER_HOME=...\apache-jmeter-5.6.3
scripts\run-load.bat
python scripts\analyze-load.py load\total.csv
scripts\run-stress.bat
```

